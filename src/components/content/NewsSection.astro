---
import type { CollectionEntry } from 'astro:content';
import BlogPostListItem from '../ui/BlogPostListItem.astro';

export interface Props {
  newsItems: CollectionEntry<'blog'>[];
  blogItems: CollectionEntry<'blog'>[];
}

const { newsItems, blogItems } = Astro.props;
---

<section
  class="bg-base mt-4 border-t border-tklab-secondary-200 bg-tklab-secondary-50 py-10"
  id="news"
>
  <h1 class="text-30xl py-10 text-center font-bold text-tklab-secondary-900">
    News & Blog
  </h1>
  <div class="max-w-50xl mx-auto px-4">
    <div class="grid grid-cols-1 gap-8 lg:grid-cols-2">
      <!-- ニュース -->
      <div>
        <h2 class="mb-6 text-2xl font-semibold text-tklab-primary-900">
          ニュース
        </h2>
        <div class="space-y-4">
          {
            newsItems
              .slice(0, 3)
              .map((post) => <BlogPostListItem post={post} variant="news" />)
          }
        </div>
      </div>

      <!-- ブログ -->
      <div>
        <h2 class="mb-6 text-2xl font-semibold text-tklab-primary-900">
          ブログ
        </h2>
        <div class="space-y-4">
          {
            blogItems
              .slice(0, 3)
              .map((post) => <BlogPostListItem post={post} variant="blog" />)
          }
        </div>
      </div>
    </div>
    <div class="container mx-auto my-4 flex justify-center py-4">
      <a href="/blogs/" class=""
        ><div class="btn btn-primary mx-auto">
          <p class="text-2xl">ブログを見る</p>
        </div>
      </a>
    </div>
  </div>
</section>
